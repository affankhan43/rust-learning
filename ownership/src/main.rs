fn main() {
    

    //ownership rules:
    // 1. Each value in RUST has a variable that called it's owner.
    // 2. There will be only one owner at a time.
    // 3. When the owner goes out of the scope,  the value will be dropped.
}
